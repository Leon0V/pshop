export default function Division({ category }) {
    return (
        <div class="container">
            <header class="d-flex justify-content-center py-3">
                <h5 class="display-5 fw-bold">{category}</h5>
            </header>
        </div>
    );
}
